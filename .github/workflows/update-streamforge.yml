name: Update StreamForge site

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/update-streamforge.yml'

permissions:
  contents: write

jobs:
  update-site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Add free audit funnel
        run: python scripts/patch_audit.py

      - name: Commit update
        run: |
          git config user.name "StreamForge Bot"
          git config user.email "actions@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Add free thumbnail audit funnel"
            git push
          fi
